# Technical Debt Register

Populate this during Phase 0 discovery and as debt is found later. Format per entry:

```
## Title
Where: file/module
Impact: what it costs us (perf, security, maintainability, SEO...)
Fix: proposed remediation (or "defer until X")
Found: YYYY-MM-DD
```

Nothing logged yet — Phase 0 discovery (MASTER-PROMPT §2, §47, §50) hasn't been run against the
copied codebase. The inherited `../backend/CLAUDE.md` §25 ("Known Issues") and §26 ("Future
Improvements") are the best current starting point until this register is populated directly.
